<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product List</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Tùy chỉnh sản phẩm */
    .product-card {
      border: 3px solid green; /* Đường viền 3px màu xanh lá */
      background-color: #d4edda; /* Nền màu xanh lá nhạt */
      color: black; /* Chữ màu đen */
      text-align: center; /* Căn giữa nội dung trong card */
      transition: background-color 0.3s ease, transform 0.3s ease; /* Hiệu ứng chuyển đổi mượt */
    }

    /* Hiệu ứng hover */
    .product-card:hover {
      background-color: #f8d7da; /* Nền màu đỏ nhạt */
      transform: scale(1.05); /* Tăng kích thước một chút */
    }

    .product-card .card-title {
      font-weight: bold;
      font-size: 1.5rem; /* Tăng kích thước chữ */
    }

    /* Tùy chỉnh màu nút bấm */
    .btn-primary {
      background-color: #007bff; /* Màu xanh dương */
      border-color: #007bff; /* Viền màu xanh dương */
    }

    .btn-primary:hover {
      background-color: #0056b3; /* Màu xanh dương đậm khi hover */
      border-color: #004085; /* Viền màu xanh dương đậm khi hover */
    }
  </style>
</head>
<body>
  <div class="container mt-5">

    <!-- Tiêu đề -->
    <h1 class="text-center mt-4 mb-4"><%= t ".Product" %></h1>

    <!-- Nút tạo sản phẩm hoặc đăng nhập -->
    <div class="d-flex justify-content-center mb-3">
      <% if authenticated? %>
        <%= link_to "New product", new_product_path, class: "btn btn-primary me-2" %>
      <% else %>
        <%= link_to "Login", new_session_path, class: "btn btn-secondary" %>
      <% end %>
    </div>

    <!-- Danh sách sản phẩm -->
    <div id="products" class="container">
      <div class="row">
        <% @products.each do |product| %>
          <div class="col-md-4 col-sm-6 mb-4">
            <div class="card h-100 shadow-sm product-card">
              <div class="card-body">
                <h5 class="card-title">
                  <%= link_to product.name, product, class: "stretched-link text-decoration-none text-dark" %>
                </h5>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>

  </div>

  <!-- Bootstrap JS (Optional, nếu cần dùng các tính năng JS của Bootstrap) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
